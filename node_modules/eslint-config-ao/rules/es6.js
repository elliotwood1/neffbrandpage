const { ERROR, OFF } = require('./eslint-consts');

module.exports = {
	rules: {
		'generator-star-spacing': [ERROR, { before: false, after: true }], //enforce the position of the * in generator functions
		'no-var': OFF //require let or const instead of var
	}
};
